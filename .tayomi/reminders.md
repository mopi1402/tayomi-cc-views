- Après validation live : publier cc-views 0.1.1 sur npmjs (pnpm publish), repasser la dep tayomi sur ^0.1.1 registry npmjs (lockfile nettoyé), décider si tayomi/views/welcome.view généré est gitignoré
- Implémenter feat/banner-kinds (.tayomi/specs/feat/banner-kinds.md) : porteur par citation `> [!TOKEN]`, directive @text, une seule banner.view pour tous les kinds. Débloqué, sa dépendance carrier-guards est prouvée. Le contrat n'est jamais passé par write-contract, vérifier son format d'abord.
- Implémenter fix/style-restore (.tayomi/specs/fix/style-restore.md) : trois sites émettent un terminateur qui efface au lieu de restaurer la couleur en cours (renderCode et chip dans style.ts, la portée bold de cell dans decorator.ts). Contrat en profil FIX à reformater avant.
- Exporter des builders de couleur (`ansi256(n)`, `rgb(r,g,b)`) pour `extendTags` : `indexed` existe déjà en privé dans style.ts, et ce sont les deux seules écritures dont chipOf dérive le chip et le cap
