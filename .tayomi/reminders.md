- Réparer la vue qui s'affiche en source au lieu du rendu quand un message porte plusieurs blocs : `VIEW_OPEN` (`src/carrier/scan.ts:13`) exige le saut de ligne, donc un flush coupant la ligne d'ouverture rend `cutUnclosedBlock` aveugle et la fence part brute sans retour possible ; l'élargir aux ouvertures incomplètes, couvrir par une suite de convergence à 2, 9, 17 et 33 morceaux (reproduit le 2026-08-01)
- Réparer le bloc jamais fermé qui disparaît entièrement de l'écran : `cutUnclosedBlock` coupe aussi sur le flush final, où plus rien n'arrivera, 59 caractères perdus sans trace sur le cas témoin ; conditionner la coupe au flush non final, fail-open comme le décorateur, couvrir par des tests de collisions de flushes concurrents (reproduit le 2026-08-01)
- Après validation live : publier cc-views 0.1.1 sur npmjs (pnpm publish), repasser la dep tayomi sur ^0.1.1 registry npmjs (lockfile nettoyé), décider si tayomi/views/welcome.view généré est gitignoré
- Trancher un échappement pour écrire un nom de tag connu entre doubles accolades dans la PROSE d'un message sans que renderTags le mange et laisse la couleur ouverte (candidats : protéger les code spans, ou une séquence d'échappement) ; le slot de ton élargit la palette donc le cas est plus fréquent
- Avertir dans `sandbox/README.md` qu'on n'y code pas : la gate TAYOMI y est aveugle et muette, donc le piège ne se signale jamais
